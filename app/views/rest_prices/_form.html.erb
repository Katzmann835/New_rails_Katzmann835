<%= form_with(model: rest_price) do |form| %>
  <% if rest_price.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rest_price.errors.count, "error") %> prohibited this rest_price from being saved:</h2>

      <ul>
        <% rest_price.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :prices_id, style: "display: block" %>
    <%= form.text_field :prices_id %>
  </div>

  <div>
    <%= form.label :resturants_id, style: "display: block" %>
    <%= form.text_field :resturants_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
